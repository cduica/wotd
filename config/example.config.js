
// this stuff should be in a .env or something but it's here for simplicity

const config = {
    host: '',
    port: 8080, 
    mongo: {
        host: 'mongodb://localhost:27017/test',
        port: 27017
    }
}

export default config;